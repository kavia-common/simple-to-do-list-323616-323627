# Optional: set this to your backend base URL (scheme + host + optional port)
# If not set, the app will call same-origin paths like /tasks (use CRA proxy in dev).
REACT_APP_API_BASE_URL=http://localhost:3001

